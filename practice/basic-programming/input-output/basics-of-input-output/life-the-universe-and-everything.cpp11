#include <bits/stdc++.h>
using namespace std;

#define endl '\n'

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);

  string s;
  while (cin >> s && s != "42") cout << s << endl;

  return 0;
}

